# Work-From-Home Activity Report

**Project:** ICT Support Ticketing System — Intelligent Service Request Monitoring and Analysis Platform
**Repository:** mmeguizo/ictFinalSystem
**Report Date:** March 13, 2026
**Last Commit / PR:** [#9 — 3.13.26](https://github.com/mmeguizo/ictFinalSystem/pull/9) (merged March 13, 2026)
**Author:** Mark Oliver Meguizo

---

| Activities | Output |
|------------|--------|
| **Gemini AI Integration** — Integrated Google Gemini 2.0 Flash AI into the ticket submission workflow. Created the full `backend/src/modules/ai/` module (`gemini.service.ts`, `ai.types.ts`, `ai.resolvers.ts`). Added `GEMINI_API_KEY` config and MySQL fulltext index on `Ticket(title, description)`. Created `ai.service.ts` on the frontend. Added "Analyze with AI" button, AI Analysis panel, Similar Tickets panel, Related KB Articles panel, and "Apply to Description" button on the submit-ticket page. | Working AI-assisted ticket creation with Gemini analysis (cleaned description, summary, category, priority, root cause, solutions). Similar resolved tickets and related Knowledge Base articles shown automatically. AI auto-suggests priority with user override option. |
| **Knowledge Base System** — Built a full Knowledge Base (FAQ) system. Added `KnowledgeArticle` model to Prisma schema with fulltext index (migration `20260310015610`). Created the full `backend/src/modules/knowledge-base/` module (`kb.repository.ts`, `kb.service.ts`, `kb.resolvers.ts`, `kb.types.ts`). Seeded 6 common ICT FAQ articles across 5 categories. Created `knowledge-base.service.ts` and the full knowledge-base page (`.ts`, `.html`, `.scss`) on the frontend. | Fully functional Knowledge Base at `/knowledge-base` with card grid view, category filters, search, article detail modal, and admin WYSIWYG editor. Admins and department heads can create/edit articles; all users can search and read. |
| **Analytics Page with Charts** — Built a dedicated Analytics page with charts, trends, and data export. Created `analytics.service.ts` with queries for `ticketAnalytics`, `slaMetrics`, `ticketTrends`, and `staffPerformance`. Updated `analytics.page.ts/html/scss` with 4 tabs: Overview (pie/bar charts), SLA Dashboard (overdue list, compliance circle), Trends (line charts, workload bar), and Staff Performance (table). Added date range picker. Created `export.service.ts` with PDF (jsPDF) and Excel (xlsx) export. | Analytics page at `/analytics` with 4 fully charted tabs for ticket overviews, SLA compliance, trends, and staff performance. PDF and Excel export functionality for management reports. |
| **SLA Enforcement — Cron Job & Escalation** — Implemented automated SLA breach detection and escalation. Created `sla-cron.service.ts` with a `node-cron` job that runs every 5 minutes to detect overdue tickets and notify assigned staff, department head, and admin. Added 2-level escalation (`escalatedAt`, `escalationLevel` fields, migration `20260224065532`) and performance indexes (migration `20260304054422`). Updated `ticket.repository.ts` to auto-calculate `actualDuration` on RESOLVED/CLOSED. | Automated SLA breach notifications sent every 5 minutes. Ticket `actualDuration` automatically recorded on resolution. SLA compliance rate, overdue list, and average resolution metrics visible on the Analytics page SLA tab. |
| **Smart Priority Suggestion** — Built a client-side NLP keyword engine to auto-suggest ticket priority. Created `priority-suggestion.ts` with weighted keyword rules (e.g., "server down", "urgent" → HIGH), category weight modifiers, and confidence scoring. Updated `submit-ticket.page.ts/html` to show the suggestion with a confidence badge as the user types. | Real-time priority suggestion displayed on the ticket creation form with a confidence percentage indicator. Users can accept the suggestion or manually override it. |
| **Satisfaction Survey** — Added a post-resolution satisfaction rating feature. Added `satisfactionRating` (1–5) and `satisfactionComment` fields to the Ticket model. Added `submitSatisfaction` GraphQL mutation restricted to the ticket creator when status is RESOLVED or CLOSED. Updated `ticket-detail.page.ts/html` with a star rating modal. | Ticket creators can rate their resolved/closed tickets with a star rating and optional comment. Rating data stored in the database and available for future analytics. |
| **Real-Time Dashboard** — Upgraded the dashboard from 60-second polling to WebSocket-driven real-time updates. Replaced polling in `dashboard.page.ts/html/scss` with `RealtimeService` Angular signals. Added a live connection dot indicator and pulse animations when stat card values change. Retained a 5-minute fallback polling safety net. Scoped the SLA reminder popup to `USER` role only. | Dashboard stat cards now update instantly when tickets are created, assigned, or resolved via WebSocket. Live connection indicator and animated counter updates improve situational awareness for staff. |
| **Bug Fixes** — Fixed four issues found during testing: (1) SLA tracker stuck on Assignment step — `manualAssign()` now creates a `DIRECTOR_APPROVED → ASSIGNED` status history entry. (2) Ticket creator's SLA tracker not refreshing in real-time — `assignUser()` now publishes `TICKET_STATUS_CHANGED`. (3) Gemini AI JSON truncation on long responses — increased `maxOutputTokens` from 1024 to 2048 and added `repairTruncatedJson()` recovery. (4) Developer cannot start work on SCHEDULED tickets — added `SCHEDULED` to workable statuses and added "Start Work" button in the UI. | All four bugs resolved. SLA tracker updates correctly on assignment; real-time status events reach ticket creators; Gemini AI handles long responses without truncation errors; developers can now start and update SCHEDULED tickets. |
| **Admin Panel Enhancements** — Updated `admin.page.ts/html/scss` with an improved user management table, user deactivation status display, and enhanced role assignment and CRUD operations. | Improved admin panel at `/admin` for managing system users, viewing active/inactive status, and updating roles. |
| **Documentation** — Created `docs/USER_MANUAL.md` (899-line complete user manual covering all features and roles). Created the in-app documentation page at `/docs` (`docs.page.ts/html/scss`). Updated `CHANGELOG.md` with full version history (v1.0.0 → v2.1.0) and updated `PROJECT_STATUS.md` with current completion status for all feature chunks. | Complete user manual accessible in-app at `/docs` and as a standalone file at `docs/USER_MANUAL.md`. Up-to-date changelog and project status tracker for all stakeholders. |
