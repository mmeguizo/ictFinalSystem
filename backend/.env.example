DATABASE_URL="mysql://root:root@localhost:3306/ictsupport"
AUTH0_DOMAIN="dev-hte6ekrcmpejgmww.au.auth0.com"
# Optional audience if you configured an API in Auth0; leave empty otherwise
AUTH0_AUDIENCE="https://your-api-identifier"

# Public base URL used to build absolute URLs for uploaded files
PUBLIC_BASE_URL="http://localhost:4000"

# Comma-separated list of allowed origins for CORS
CORS_ORIGIN="http://localhost:4200"
